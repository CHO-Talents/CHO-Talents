# TASK-083 테스트 시나리오

| ID | 입력/조건 | 기대 결과 | 확인 방법 |
|---|---|---|---|
| TC-083-01 | 학생용 활성 항목 `SGS 포토존 미션 시상`, 이모지 `📸`, 순서 7 등록 | 학생 탭에 이름·이모지·지급 금액·규칙·설명 카드가 추가된다 | `earn-talents.html`에서 `fetchTalentItems('student')` 결과를 카드로 렌더링하는지 확인 |
| TC-083-02 | 학생/교사 활성 항목을 각각 등록 | 각 탭에는 대상 유형이 같은 항목만 `sort_order` 순서대로 보인다 | `js/talent.js`의 대상 필터와 카드 렌더링 호출 확인 |
| TC-083-03 | 항목을 비활성화 | 적립 안내 카드에서 해당 항목이 사라진다 | `fetchTalentItems()`의 `is_active=true` 조건 확인 |
| TC-083-04 | 항목 추가/수정 모달에 이모지 입력 | 저장 행에 `emoji`가 포함되고 관리 목록과 카드에 같은 값이 표시된다 | `admin/talent-items.html`의 폼·저장·목록 코드 확인 |
| TC-083-05 | 기존 운영 DB에 TASK-083 SQL 적용 | `emoji` 컬럼, 기본값, 기존 항목용 이모지 보정이 적용된다 | SQL Editor에서 `docs/TASK-083_talent_item_emoji.sql` 실행 후 조회 |
| TC-083-06 | 이모지 미입력 | 기본 이모지 `✨`가 저장·표시된다 | 모달 초기값 및 저장 기본값 확인 |
