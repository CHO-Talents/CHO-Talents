# TASK-106 테스트 시나리오 — 즐겨찾기 전체 메뉴 동기화 및 운영 문서 최신화

**버전:** v4.2.9  
**작성일:** 2026-08-19

| ID | 검증 항목 | 입력/확인 방법 | 기대 결과 |
|---|---|---|---|
| T1 | 네비게이션-즐겨찾기 경로 정합성 | `js/nav.js`의 `NAV_MENU` href와 `index.html`의 `ALL_SHORTCUTS` href를 정적 비교 | 네비게이션 메뉴 경로가 모두 즐겨찾기 후보에 존재. 역할별 가이드 2개는 상단의 단일 가이드 자동 라우팅을 보완하는 추가 후보로 허용 |
| T2 | 추가 및 기존 항목의 최소 권한 | 공지/월별 달란트/구매 통계/Slack 룰/서비스 통계, 사용자/관리자 관리, 버전, 관리자 가이드의 `minRank`를 `NAV_MENU.minPerm` 또는 실제 페이지 권한과 대조 | 각각 40/40/60/80/80/80/90/100/100으로 일치 |
| T3 | 문서·가이드 현재 버전 | 3개 문서와 11개 가이드에서 `v4.2.9` 존재 여부, 이전 현재 기준 표기 검색 | 모든 대상에 `v4.2.9`가 있고 `현재 안내 기준 v3.79`, `문서 기준 버전 v3`, `작성 기준 v3.89`가 없음 |
| T4 | 지급 대상일 문서 일관성 | README, 사용자 안내서, 아키텍처, 역할/운영 가이드에서 `지급 대상일`, `grant_date`, `다음 일요일` 문맥 검토 | 일요일 당일·월~토요일 다음 일요일, QR 발생 시각과 지급 대상일 분리, TASK-104/105 적용 필요성이 일관됨 |
| T5 | JavaScript 정적 문법 | `node --check js/version.js`, `js/nav.js`, `js/talent.js` 실행 | 구문 오류 없음 |
| T6 | 변경 안전성 | `git diff --check`, 수정 파일 UTF-8 BOM 검사, 작업 트리 범위 확인 | 공백 오류·BOM 없음, 기존 지급 기능/SQL 사용자 수정은 보존 |

## 수동 브라우저 확인 권장

1. 40/60/80/100 권한 계정으로 메인 `⭐ 즐겨찾기 설정`을 열어 해당 항목만 보이는지 확인합니다.
2. 선택·저장 뒤 새로고침 및 다른 기기 로그인에서 카드가 유지되는지 확인합니다.
3. 월~토요일 일반 지급과 QR 수령 후 `grant_date`가 다음 일요일이고, 일요일 지급은 당일인지 운영 DB에서 확인합니다.
